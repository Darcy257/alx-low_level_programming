#include <stdio.h>

/**
 * main - print allbase 10 single digits
 *
 * Return: always 0 for success
 */

int main(void)
{
int i;
for (i = 0 ; i < 10 ; i++)
	printf("%d", i);
printf("\n");
return (0);
}
